generated: '2026-08-13'
method: searched
source: https://github.com/chordcommerce/chord-copilot/tree/main/plugin/skills
note: >-
  These four Agent Skills are PROVIDER-AUTHORED and saved verbatim — nothing here
  is generated by API Evangelist. Chord ships them as a Claude Code plugin
  (marketplace `chord`, plugin `chord`) whose .mcp.json registers the hosted MCP
  server at https://mcp.chord.co/mcp. Every tool the skills name is an
  mcp__chord__* tool on that server; the daily-insights skill additionally
  depends on an mcp__slack__* server the customer supplies. Files are copied
  byte-for-byte from plugin/skills/<name>/SKILL.md with only the filename
  prefixed for this repo.
plugin:
  repo: https://github.com/chordcommerce/chord-copilot
  marketplace: .claude-plugin/marketplace.json
  plugin_manifest: plugin/.claude-plugin/plugin.json
  plugin_name: chord
  version: 0.1.0
  license: MIT
  install: /plugin marketplace add chordcommerce/chord-copilot
  mcp_server: https://mcp.chord.co/mcp
skills:
  - name: chord-copilot
    file: chord-commerce-copilot.md
    upstream: plugin/skills/copilot/SKILL.md
    summary: >-
      The default retrieval-grounded SQL workflow — search_schema →
      search_saved_views / search_sql_pairs → search_instructions → draft SQL →
      execute_sql — for any warehouse data question.
    tools:
      - search_schema
      - search_saved_views
      - search_sql_pairs
      - search_instructions
      - search_documentation
      - preview_table
      - execute_sql
      - list_tenants
      - switch_tenant
  - name: chord-activation-health
    file: chord-commerce-activation-health.md
    upstream: plugin/skills/activation-health/SKILL.md
    summary: >-
      Diagnose the activation layer: which destinations are connected, which
      syncs are active/errored/paused, when each last succeeded, and why a
      Klaviyo list or Meta audience stopped updating.
    tools:
      - list_data_sources
      - list_audience_syncs
      - get_sync_run_history
      - get_activation_summary
  - name: chord-daily-insights
    file: chord-commerce-daily-insights.md
    upstream: plugin/skills/daily-insights/SKILL.md
    summary: >-
      Nightly digest across five commerce signals (revenue health, channel ROAS,
      repeat rate / LTV, LTV:CAC efficiency, VIP slippage), threshold-flagged,
      narrated, and posted to Slack. Designed to run unattended on a cron.
    tools:
      - execute_sql
      - search_instructions
      - search_saved_views
      - mcp__slack__slack_send_message
    note: Requires a customer-supplied Slack MCP server; Chord does not host one.
  - name: chord-metric-verify
    file: chord-commerce-metric-verify.md
    upstream: plugin/skills/metric-verify/SKILL.md
    summary: >-
      A pre-flight verification pass over an already-produced number — checks
      revenue basis (gross vs net), order-status filter, and UTC/local timezone
      alignment with Shopify admin before the figure leaves the room.
    tools:
      - ask
      - execute_sql
      - search_instructions
